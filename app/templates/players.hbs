<div class="home admin">
	<div class="players">
		{{#ember-wormhole to="buttons"}}
			<span class="title">Players</span>
		{{/ember-wormhole}}
		{{#each player in model}}
			<div class="player">
				<span class="title">{{player.username}}</span>
				<div class="field" {{action "toggleAdmin"}}>
					<span class="admin">admin</span>
					<i class="fa {{if player.isAdmin 'fa-check-circle-o' 'fa-circle-o'}}"></i>
				</div>
				<div class="characters">
					<table border="0" cellspacing="0" cellpadding="0">
						<thead>
							<tr>
								<th>Name</th><th>Race</th><th>Class</th><th>Level</th><th>Actions</th>
							</tr>
						</thead>
						<tbody>
							{{#each char in player.chars}}
								<tr {{action "openCharacter" char}}>
									<td>{{char.name}}</td>
									<td>{{char.race}}</td>
									<td>{{char.class}}</td>
									<td>{{char.level}}</td>
									<td class='actions'>
										<span class="edit" title="edit" {{action "editChar" player char}}>e</span>
										<span class="remove" title="remove" {{action "removeChar" player char}}>x</span>
									</td>
								</tr>
							{{/each}}
						</tbody>
					</table>
				</div>
				<div class="controls">
					<div class="new-char" {{action "linkCharacter"}}>
						Link Character
					</div>
					<div class="remove-player" {{action "removePlayer"}}>
						Remove player
					</div>
				</div>
			</div>
		{{/each}}
	</div>
</div>
